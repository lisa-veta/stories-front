import { Container } from '@pages/EditorPage/ui/EditorPage.styles.ts';
import { SettingsPanel } from '@widgets/SettingsPanel';


export const EditorPage = () => {
  return (
    <Container>
      <SettingsPanel></SettingsPanel>
    </Container>
  );
};
